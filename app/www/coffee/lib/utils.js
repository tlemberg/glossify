// Generated by CoffeeScript 1.9.1
(function() {
  define(function() {
    var _appHeight, _appWidth, _logout, _randomInt, _stripNumeric, _windowHeight, _windowWidth, _withUnit;
    _stripNumeric = function(s) {
      return s.replace(/[^-\d\.]/g, '');
    };
    ({
      _shuffle: function(arr) {
        var i, j, tempi, tempj;
        i = arr.length;
        if (i === 0) {
          return false;
        }
        while (--i) {
          j = Math.floor(Math.random() * (i + 1));
          tempi = arr[i];
          tempj = arr[j];
          arr[i] = tempj;
          arr[j] = tempi;
        }
        return arr;
      }
    });
    _randomInt = function(min, max) {
      return Math.floor(Math.random() * max) + min;
    };
    _logout = function() {
      removeLocalStorageItem('user_profile');
      return removeLocalStorageItem('dictionary_fr');
    };
    _withUnit = function(measure, unit) {
      return "" + measure + unit;
    };
    _appWidth = function() {
      return _stripNumeric($(".page").first().css('width'));
    };
    _appHeight = function() {
      return _stripNumeric($(".page").first().css('height'));
    };
    _windowWidth = function() {
      return window.innerWidth;
    };
    _windowHeight = function() {
      return window.innerHeight;
    };
    return {
      stripNumeric: function(s) {
        return _stripNumeric(s);
      },
      shuffle: function(arr) {
        return _shuffle(arr);
      },
      randomInt: function(min, max) {
        return _randomInt(min, max);
      },
      logout: function() {
        return _logout();
      },
      withUnit: function(measure, unit) {
        return _withUnit(measure, unit);
      },
      appWidth: function() {
        return _appWidth();
      },
      appHeight: function() {
        return _appHeight();
      },
      windowWidth: function() {
        return _windowWidth();
      },
      windowHeight: function() {
        return _windowHeight();
      }
    };
  });

}).call(this);
