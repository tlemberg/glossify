// Generated by CoffeeScript 1.9.1
(function() {
  define(function() {
    return {
      stripNumeric: function(s) {
        return s.replace(/[^-\d\.]/g, '');
      },
      shuffle: function(arr) {
        var i, j, tempi, tempj;
        i = arr.length;
        if (i === 0) {
          return false;
        }
        while (--i) {
          j = Math.floor(Math.random() * (i + 1));
          tempi = arr[i];
          tempj = arr[j];
          arr[i] = tempj;
          arr[j] = tempi;
        }
        return arr;
      },
      randomInt: function(min, max) {
        return Math.floor(Math.random() * max) + min;
      },
      logout: function() {
        removeLocalStorageItem('user_profile');
        return removeLocalStorageItem('dictionary_fr');
      },
      withUnit: function(measure, unit) {
        return "" + measure + unit;
      },
      appWidth: function() {
        return this.stripNumeric($(".page").first().css('width'));
      },
      appHeight: function() {
        return this.stripNumeric($(".page").first().css('height'));
      },
      windowWidth: function() {
        return window.innerWidth;
      },
      windowHeight: function() {
        return window.innerHeight;
      }
    };
  });

}).call(this);
