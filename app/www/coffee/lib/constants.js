// Generated by CoffeeScript 1.9.1
(function() {
  define(['jquery', 'hbs!../../hbs/src/login', 'hbs!../../hbs/src/signup', 'hbs!../../hbs/src/overview', 'hbs!../../hbs/src/study', 'login', 'signup', 'overview', 'study'], function($, loginTemplate, signupTemplate, overviewTemplate, studyTemplate, login, signup, overview, study) {
    var _pageActionMap, _pages, _templateMap;
    _pageActionMap = {
      login: {
        load: login.loadPage,
        refresh: login.refreshPage
      },
      signup: {
        load: signup.loadPage,
        refresh: signup.refreshPage
      },
      overview: {
        load: overview.loadPage,
        refresh: overview.refreshPage
      },
      study: {
        load: study.loadPage,
        refresh: study.refreshPage
      }
    };
    _templateMap = {
      login: loginTemplate,
      signup: signupTemplate,
      overview: overviewTemplate,
      study: studyTemplate
    };
    _pages = ['login', 'signup', 'overview', 'study'];
    return {
      pageActionMap: _pageActionMap,
      templateMap: _templateMap,
      pages: _pages
    };
  });

}).call(this);
